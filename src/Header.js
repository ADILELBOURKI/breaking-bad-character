import React from "react";
import bb from "./utils/bb.png";

const Header = () => {
  return (
    <header className="center">
      <img src={bb} alt="breaking bad logo " />
    </header>
  );
};

export default Header;
